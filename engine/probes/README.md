# Probes

Scripts that touch the live API, or that exist to answer one question about it. Anything a
session writes and runs which would be useful to a later session lives here rather than in a
scratch directory. A probe that only exists in a session transcript is a probe that gets
rewritten from scratch next month.

This is not the test suite, and since 2026-09-20 it does not live inside it either. Probes sit at
`engine/probes/` rather than under `engine/tests/`, so `testpaths = ["tests"]` cannot reach them and
`tests/` means gates and nothing else. Probes are not run by `uv run pytest`, they are not
assertions, and they are not a gate. They are reproducible evidence-gathering tools.

## Rules

- **Read only unless the user asks otherwise.** A probe that writes to an account must say so in
  its first docstring line and must not run by default.
- **Credentials come from `.env` at the repository root**, never from arguments, never inline.
- **Every probe writes a log to `../logs/`** on success and on failure, with the naming and
  sanitising rules in [../logs/README.md](../logs/README.md).
- **Pace at 2850 ms between live requests**, the measured 0.351 req/s anchor, unless the probe
  exists specifically to test pacing.
- **State the request count** in the probe's header, so the cost of running it is visible before
  it runs.

## Index

| Script | Requests | What it answers |
|---|---|---|
| `end_to_end_read.py` | 2 | Are the library's own layers wired to each other. Drives `Session`, `HttpxTransport`, `PacedSender`, `bootstrap`, the request builder and the classifier through `_core.smoke.read_one_thread_page`, rather than reimplementing the request shape. Ran 2026-09-21, 20 edges, 2956 ms for both requests. Run it with `uv run python probes/end_to_end_read.py`, no `--no-project`, because it imports the library. |
| `adopt_and_save.py` | 2 | Does an adopted session survive a process boundary, first half. Adopts from `.env`, bootstraps, reads one page to prove the credentials are alive, saves to `engine/state/session.json` and exits. Ran 2026-09-21, 2997 ms, 734-byte session file at `-rw-------`, reloaded equal in memory. Run it with `uv run python probes/adopt_and_save.py`, no `--no-project`. |
| `reload_and_call.py` | 1, or 2 if the stored tokens are stale | The Phase 1 stop condition, second half. Loads the session file in a process that never saw the first probe, and reads one page with no credential from `.env` at all. Ran 2026-09-21, 432 ms, one request, no re-bootstrap, 20 edges. Ran again 2026-09-23 for 17.8 item 8, 460 ms, one request, the stored tokens 10338 s old accepted with no re-bootstrap. Rerunning it against an old session file is the cheapest measurement available of the open token-lifetime question. Run it with `uv run python probes/reload_and_call.py`, no `--no-project`. |
| `message_node_shape.py` | 1, or 2 if the stored tokens are stale | What the message node actually carries. Loads the saved session, reads one page through the library and records the key union of all twenty nodes plus the edge, the connection and the thread, with types, counts and lengths but no values except `__typename`, `content_type` and the first four characters of an id. Ran 2026-09-21, 308 ms, one request, 28 node keys, and it closed both open field-pair questions: `id` equals `message_id` on 20 of 20, and only `reactions` carries the emoji. Run it with `uv run python probes/message_node_shape.py`, no `--no-project`. |
| `feed_node_shape.py` | 1, or 2 if the stored tokens are stale | What a timeline feed node actually carries. Loads the saved session, reads one page through the library and records the key union of the connection, the edge and every `media` node, plus a census of which union slot each item filled, with types, counts and lengths but no values except `__typename`, `media_type`, `product_type` and the like. Ran 2026-09-21, one request, 15 edges of which 6 were media, every edge cursor null, exactly one union slot filled per item, and it is the evidence `models/feed.py` was written from. Run it with `uv run python probes/feed_node_shape.py`, no `--no-project`. |
| `home_document_preloader.py` | 1, or 2 with `--follow` | Does the first feed page arrive inside the home document when the library loads it, rather than a browser. Drives `_core.feed.read_first_feed_page_from_document` and records item count and kinds, `has_next_page`, the `end_cursor` length and whether the page tokens changed. `--follow` then reads page two through the pagination query with the document's cursor. Ran 2026-09-23 twice: 4 items each time, and with `--follow` page two carried 6 items with `has_next_page` true. It is the replay evidence for finding `home-timeline-first-page-preloader`. Run it with `uv run python probes/home_document_preloader.py`, no `--no-project`. |
| `home_load_cookie_sync.py` | 10 at most, 2 of them to facebook.com, writes once | Does the engine's own home load send its companions and then the four-request cookie sync tail as designed in 17.2.3 of the build plan. Drives `AsyncClient.feed()` under the default parity behavior, records every request at the transport with its host, name, status, length, classification and offsets from the document, and the names of cookies each response set, and refuses an eleventh request. `fr` is recorded as presence and length only. Posts to `/sync/instagram/` and may change the saved `fr`, so it sends nothing without `--approved`. Ran 2026-09-23 with the owner's approval: ten requests, all 200, the tail departing at 4967 to 6252 ms after the document, `fr` absent before and 126 characters after, log `home-load-cookie-sync-2026-09-23-041954.json`. It is the first engine replay of the four cookie sync findings. Run it with `uv run python probes/home_load_cookie_sync.py --approved`, no `--no-project`. |
| `capture_thread_oracle.py` | one per page, 399 on 2026-09-22 | The raw material for the recorded oracle. Reads the whole thread through the library's own request builder and classifier, writes every body verbatim to gitignored `../exports/thread-oracle-<stamp>/`, stops on `has_next_page` false or at 400 pages, and resumes from its manifest with `--resume`. Each page is also offered to the mapper and the outcome logged. Ran 2026-09-22, 1177 s, 7967 messages in 19 content types, zero duplicates, zero mapper refusals. The raw pages carry message text and the echoed `dtsg_token`, which is why they never leave `exports/`. Run it with `uv run python probes/capture_thread_oracle.py`, no `--no-project`. |
| `thread_route_live.py` | 2, or 3 if the stored tokens are stale | Does the default thread route work live. Opens the thread through `IGDThreadDetailQuery` via `_core.direct.read_thread_messages`, then reads the next older page through `IGDMessageListOffMsysQuery` with the detail answer's cursor. Ran 2026-09-23, 3043 ms, both friendly names sent, 20 and 20 messages, both `has_next_page` true, no message on both pages, the older page entirely older. Run it with `uv run python probes/thread_route_live.py`, no `--no-project`. |
| `like_discovery.py` | 8, 9 with a bootstrap, 4 of them writes | Step 15's discovery, done from the engine side because ruling 23 allowed no browser load. Reads one own post off the timeline, reads it by shortcode, sends the write that moves it away from its starting like state twice, reads, sends the reversing write twice, reads, all built by `build_graphql_request` and written through `send_write`. It carries the three `doc_id` values itself, since none was verified when it ran. It answered which identifier the mutations take (the `pk`), what they echo (the `<pk>_<owner id>` form and `has_liked`), and that a repeat of either converges. Ran 2026-09-23 with the owner's advance approval: eight requests, the post read true 6, false 5, true 6, and ended liked as it started. Log `like-discovery-2026-09-23-051806.json`. Refuses to run without `--approve like-cycle`. Run it with `uv run python probes/like_discovery.py --approve like-cycle`, no `--no-project`. |
| `like_cycle.py` | 5, 6 with a bootstrap, 2 of them writes | Step 15's live acceptance through the public `AsyncClient`: `post`, the write away from the starting state, `post`, the reversing write, `post`. Refuses to write unless the post is the viewer's own, and caps itself at six requests. Ran 2026-09-23: five requests, `unlike` then `like` because the owner's post was already liked, reads true 6, false 5, true 6, ended in its starting state. Log `like-cycle-2026-09-23-052731.json`. Refuses to run without `--approve like`. Run it with `uv run python probes/like_cycle.py --approve like --code CODE`, no `--no-project`. |
| `comment_discovery.py` | 2, 1 and 7 over three stages, one more each with a bootstrap, writes in two | Step 16's discovery, done from the engine side because ruling 23 allowed no browser load. `--stage scout` reads the comment page and sends the delete mutation with an input naming no comment, `--stage coerce` sends it with `comment_id` "0" and `media_id`, and `--stage cycle` creates, reads, deletes, reads, creates, deletes and reads, falling back once to the REST delete for a comment a delete did not remove. It carries the three `doc_id` values itself, since none was verified when it ran. It answered which identifier every call takes (the media `pk`), the create's `data` shape and answer, and the delete's input field names, which the compiled artifact does not carry: the empty input was refused as `noncoercible_variable_value`, the two names coerced, and a delete naming no comment answered a null root. Ran 2026-09-23 with the owner's advance approval: 2, 1 and 7 requests, both comments listed after their create and gone after their delete, none left up. Logs `comment-discovery-scout-2026-09-23-053720.json`, `comment-discovery-coerce-2026-09-23-053739.json`, `comment-discovery-cycle-2026-09-23-054051.json`. Reads `IG_COMMENT_TEXT` from the root `.env`. Refuses to run without `--approve comment-cycle`. Run it with `uv run python probes/comment_discovery.py --approve comment-cycle --stage STAGE --pk PK`, no `--no-project`. |
| `comment_cycle.py` | 5, 6 at most, 2 of them writes | Step 16's live acceptance through the public `AsyncClient`: `comments`, `comment`, `comments`, `delete_comment`, `comments`. Deletes a stray viewer comment if `comment` raises, tries the REST delete once if `delete_comment` raises, and caps itself at six requests. Ran 2026-09-23: five requests, the created id listed with the same text and the viewer as author, `created_at` 1 s from the clock, the page back to no comment. Log `comment-cycle-2026-09-23-054958.json`. Reads `IG_COMMENT_TEXT` from the root `.env`. Refuses to run without `--approve comment`. Run it with `uv run python probes/comment_cycle.py --approve comment --pk PK`, no `--no-project`. |
| `inbox_change_feed.py` | 2, or 4 with `--stage full`, 9 with `--send-arranged` (writes twice), one more with a bootstrap | Step 21 of the build plan, whether the inbox listing works as a change feed. Reads the listing through `build_inbox_listing_request`, `classify` and `parse_inbox_listing`, with one fresh iris device id for the run, and records per row the `thread_fbid`, position, pin flag, `last_activity_timestamp_ms` and newest message id, plus key-level checks of the raw payload. The default stage is reads 1 and 2, `--spacing` seconds apart (60 by default, the listener's poll interval). `--stage full` then waits at a prompt for one message the owner sends by hand, reads again, and tops up the moved thread with `newer_than_message_id`, which does not mark it seen. Ran 2026-09-23, default stage only: two requests, both 200 and 113094 bytes, 15 rows, every row identical across the two reads, log `inbox-change-feed-reads-2026-09-23-060154.json`. It is also the two engine passes on finding `direct-inbox-thread-list`. `--stage full --send-arranged --approve send-message --thread-fbid FBID [--base-message-id ID]` has the engine send the arranged message itself under ruling 30, with the text in `IG_DM_TEXT`, never logged: the send, read 4, read 5 on the target with `newer_than_message_id`, a control read 5b on another thread with a live base when read 1 listed no live message in the target, the thread open and unsend, and read 6. A checkpoint stops it with no unsend. Ran 2026-09-23 against thread 1071167815895546: nine requests, all 200, one message sent and unsent. The target was off the first page before the send, at position 0 after it with every other row one place lower, and off it again after the unsend. Read 5 on the unsent predecessor and the control read on a live base both returned exactly one message, `has_next_page` false. Log `inbox-change-feed-full-arranged-2026-09-23-161947.json`. Run it with `uv run python probes/inbox_change_feed.py`, no `--no-project`. |
| `events_live.py` | 3 per surface, 4 at most, plus 1 per page of a thread that gained messages; with `--send-arranged` 11 per surface, 12 at most, plus 3 write-side requests, 4 at most (writes twice) | Step 23's reduced live acceptance. Runs `dumpsta events --json --ids-only --duration 150 --interval 60` in process through `dumpstagram._cli.main.main` on one surface, with the client's paced sender rebuilt over a counting transport that wraps the client's own `HttpxTransport`, records per request its name, offset, status, length and whether the account's pacer slot was held, and refuses a fifth request. The printed lines carry ids and times only. Ran 2026-09-23 on both surfaces: sync, three listing reads and nothing printed, log `events-live-sync-2026-09-23-065330.json`; async, three listing reads, one thread read and two unarranged new messages printed as ids, log `events-live-async-2026-09-23-065605.json`. Every request held the slot. `--send-arranged --approve send-message --thread-fbid FBID` runs the planned acceptance instead: `--duration 600`, ten polls, and a second `AsyncClient` on its own thread that sends the arranged message 20 s after the third listing read and unsends it 20 s after the listener's next request once the event line carries its id, or after two more listing reads without it. Each printed line is timed on the same clock as the requests, and the log carries the seconds from the send's departure to the event line and the smallest gap between a write request and a listener request. Ran 2026-09-23 on both surfaces against thread 1071167815895546: each 11 listener requests holding the slot and 3 write-side requests, all 200, one event printed, 44.5 s after the send on sync and 44.1 s on async, the message unsent. Logs `events-live-sync-arranged-2026-09-23-163709.json` and `events-live-async-arranged-2026-09-23-164723.json`. Run it with `uv run python probes/events_live.py --surface sync` or `--surface async`, no `--no-project`. |
| `newer_than_pages.py` | 4, 5 with a bootstrap | E1 item 1 of the web parity plan, ruling W10: how `newer_than_message_id` behaves when more than a page of messages is newer than the base. Reads one thread's newest page and the page before it through `IGDMessageListOffMsysQuery`, takes as the base the message 25 places back, then reads with that base and once more by the answer's cursor with the same base. Records positions, counts, ids and times, never text or senders. Ran 2026-09-23: four requests, all 200, the filtered read returned the newest 20 with `has_next_page` true and the cursor read the other 4 with `has_next_page` false, 24 of 24 newer messages covered, the base on neither page. Log `newer-than-pages-2026-09-23-185050.json`. It is the fifth pass on finding `direct-thread-older-page-offmsys`. Run it with `uv run python probes/newer_than_pages.py`, no `--no-project`. |
| `iter_home_two_pages.py` | 2 to 4, one more with a bootstrap, capped at 4 | E1 item 5 of the web parity plan: `client.feeds.iter_home(limit=16)` through the public API under the EXPORT spacing, the first page by the pagination query, companions and cookie sync off. Records each request's name, status, length, offset, cursor presence and length, and the items yielded before it left, plus item kinds counted. Ran 2026-09-23: two requests, both 200, 15 items on the first page, the second read carried a 1188 character cursor 3.7 s later, 16 items yielded and no third read. Log `iter-home-two-pages-2026-09-23-210259.json`. Run it with `uv run python probes/iter_home_two_pages.py`. |
| `media_shape.py` | 1 to 10 API reads and 2 CDN fetches, 1 fetch with `--fetch image`; 3 API reads and none with `--stage post-detail` | E1 item 6 of the web parity plan: what a reel, a carousel, a slide and a reel's audio carry. Reads home timeline pages through the pagination query until one holds a video and a carousel (with `--until video-child`, also a carousel with a video slide, six pages at most), records the key union of every video node, carousel node and slide with types, counts, lengths, the ranges of dimension and duration keys and enum values only, which host family each URL sits on, and each DASH manifest's root attribute names, `mediaPresentationDuration` and representation counts. Then one cookieless CDN fetch of the smallest image of a slide and one of the smallest video rendition, recording status, header names, content length against bytes received and content type. `--stage post-detail` reads the first video and carousel of page one again through the post query. `--write-fixtures` writes pseudonymised nodes to `tests/fixtures/media/`. Ran 2026-09-23 three times: 1 API read and 2 CDN fetches, 4 reels and 2 carousels on one page, log `media-shape-2026-09-23-212358.json`; 6 API reads and 1 CDN fetch, 7 reels, 5 carousels, 23 slides and no video slide, log `media-shape-2026-09-23-212550.json`; and the post-detail stage, 3 API reads, log `media-shape-post-detail-2026-09-23-212705.json`. Every rendition on `cdninstagram.com`, no cookie needed, no redirect. Run it with `uv run python probes/media_shape.py --write-fixtures`, no `--no-project`. |
| `media_download_live.py` | 1 API read and 1 CDN fetch, capped at 2 and 1 | E1 item 6's live acceptance of `client.media.download`. Reads the first home page from the home document with companions and cookie sync off, records what the extended model mapped, downloads the smallest image rendition through the public API into a temporary directory, records the CDN fetch's host, header names, status, content type and declared length, checks a second download to the same path is refused before any fetch, and deletes the file. Ran 2026-09-23 twice: 5804 bytes the first time, with no content type or length recorded, log `media-download-live-2026-09-23-214329.json`; 6337 bytes of JPEG the second, `image/jpeg` with no `content-length`, no cookie header, the second download refused, log `media-download-live-2026-09-23-214350.json`. Run it with `uv run python probes/media_download_live.py`, no `--no-project`. |
| `note_discovery.py` | 6, 7 if close friends is refused, 2 of them writes | Step 14's discovery, done from the engine side because ruling 23 allowed no browser load: a bootstrap, a tray read, a note set with audience 1, a tray read, a delete of the note it made, a tray read, all built by `build_graphql_request` and written through `send_write`. It carries both note `doc_id` values itself, since neither had its second observation when it ran. It answered that the bootstrap's `actorID` is 17 digits and differs from `ds_user_id`, that the create accepts it as `actor_id` with audience 1 and echoes 1 in the created item and the tray, the create answer's key shape, and that the delete answers a null root with the note gone. Ran 2026-09-23 under ruling 30: six requests, the owner's song note replaced and then the new note deleted, tray 14, 14, 13 items. Log `note-discovery-2026-09-23-140920.json`. Reads `IG_NOTE_TEXT` from the root `.env` and records only its length. Refuses to run without `--approve set-note`. Run it with `uv run python probes/note_discovery.py --approve set-note`, no `--no-project`. |
| `note_write_cycle.py` | 5, 6 with a bootstrap, 2 of them writes | Step 14's live acceptance through the public `AsyncClient`: `notes`, `set_note` for close friends, `notes`, `delete_note` with the returned id, `notes`. Caps itself at six requests. Ran 2026-09-23: five requests, trays of 13, 14 and 13 items, exactly one own note with the returned id and the close friends audience in the middle, none at the end. Log `note-write-cycle-2026-09-23-141647.json`. Reads `IG_NOTE_TEXT` from the root `.env` and records only its length. Refuses to run without `--approve set-note`. Run it with `uv run python probes/note_write_cycle.py --approve set-note`, no `--no-project`. |
| `follow_discovery.py` | 2 and 9 over two stages, one more each with a bootstrap, 4 writes in the second | Step 17's discovery, done from the engine side because ruling 23 allowed no browser load, against the account the owner named, read from `IG_FOLLOW_TARGET` in the root `.env` and logged only by its numeric id. `--stage read` resolves the username through the timeline query, falling back to the profile page document, and reads the profile by id, recording `is_private`, every `friendship_status` flag and the type of every user key. `--stage cycle --user-id ID` reads, then twice sends the write that moves the relationship away from its start, reads, sends the reversing write and reads, all built by `build_graphql_request` and written through `send_write`, and refuses to start on a private account the viewer follows. It carries both `doc_id` values itself, since neither was verified when it ran. It answered that both mutations take `target_user_id` as the numeric `pk`, what each answers, that another account's profile carries a ten-flag `friendship_status` and three null flags the mapper then refused, and that the account ended as it started. Ran 2026-09-23 with the owner's approval: read 2 requests, log `follow-discovery-read-2026-09-23-142953.json`; cycle 9 requests, `following` false, true, false, true, false, log `follow-discovery-cycle-2026-09-23-143209.json`. Refuses to run without `--approve follow`. Run it with `uv run python probes/follow_discovery.py --approve follow --stage read`, then `--stage cycle --user-id ID`, no `--no-project`. |
| `follow_cycle.py` | 5, 6 with a bootstrap, 2 of them writes | Step 17's live acceptance through the public `AsyncClient`: `profile_by_id`, the write away from the starting relationship, `profile_by_id`, the reversing write, `profile_by_id`. Writes only when the first read's username is `IG_FOLLOW_TARGET` from the root `.env`, refuses a private account the viewer follows, and caps itself at six requests. Ran 2026-09-23: five requests, `follow` then `unfollow` because the viewer did not follow the account, `following` false, true, false and `outgoing_request` false throughout, ended in its starting state. Log `follow-cycle-2026-09-23-143934.json`. Refuses to run without `--approve follow`. Run it with `uv run python probes/follow_cycle.py --approve follow --user-id ID`, no `--no-project`. |
| `direct_send_discovery.py` | 1 in the inbox stage, 3 in the send stage, one more each with a bootstrap, 2 writes in the send stage | Step 18's engine-side discovery, the second observation after the browser's. `--stage inbox --user-id ID` reads the inbox's first page and records whether a thread with the owner-named account is on it, by ids only. `--stage send --user-id ID --thread-fbid FBID --thread-id THREAD_ID` sends one text message with the fourteen variables the browser's composer sent and a fresh `offline_threading_id`, reads the thread's newest page (or the page newer than `--base-message-id`), and unsends it with the 39-digit thread id, all built by `build_graphql_request` and written through `send_write`. It carries both `doc_id` values itself, since neither had its second observation when it ran. Ran 2026-09-23 with the owner's approval: inbox, 1 request, 17 rows and no thread with the account, log `direct-send-discovery-inbox-2026-09-23-145836.json`; send, 3 requests, the answer's `message_id` equal to its `id`, the read held exactly the new message with the `offline_threading_id` echoed, and the unsend answered true, log `direct-send-discovery-send-2026-09-23-152132.json`. Reads `IG_DM_TEXT` from the root `.env` and records only its length. The send stage refuses to run without `--approve send-message`. Run it with `uv run python probes/direct_send_discovery.py --stage inbox --user-id ID`, then the send stage, no `--no-project`. |
| `direct_send_cycle.py` | 5, 6 with a bootstrap, 2 of them writes | Step 18's live acceptance through the public `AsyncClient`: `send_message`, `thread_messages` with `newer_than_message_id`, `unsend_message` (the thread open, then the unsend), `thread_messages` for the newest page. No read goes before the send, because the step's budget had five requests left and the discovery run left the thread with nothing listed. Caps itself at five requests and still unsends when the newer-than read fails. Ran 2026-09-23: five requests, the newer-than read (its base the discovery run's unsent message) returned exactly the new message, found by its `offline_threading_id`, the unsend applied and the newest page after it was empty. Log `direct-send-cycle-2026-09-23-153933.json`. Reads `IG_DM_TEXT` from the root `.env` and records only its length. Refuses to run without `--approve send-message`. Run it with `uv run python probes/direct_send_cycle.py --approve send-message --thread-fbid FBID --base-message-id MESSAGE_ID`, no `--no-project`. |
| `phase3_cli_acceptance.py` | 26, two more per re-bootstrap, 40 at most, 10 of them writes | The Phase 3 stop condition's live half: every Phase 3 capability run once from the `dumpsta` command, confirmed by a read and reversed in the same run. It never imports the library. Each step is a `uv run dumpsta --json` subprocess, and `cli_request_counter/sitecustomize.py`, put on each one's `PYTHONPATH`, records every request that leaves the process at `httpx.AsyncClient._send_single_request` by host, friendly name and status, and refuses the 41st across the run. Five cycles in order of exposure: note (`note list`, `note set --audience close-friends`, `note list`, `note delete`, `note list`), like (`post`, `unlike`, `post`, `like`, `post` on pk 3879825181007222959, the order following the start state), comment (`comments`, `comment`, `comments`, `delete-comment`, `comments` on the same post), follow (`profile --by-id`, `follow`, `profile --by-id`, `unfollow`, `profile --by-id` on 6722711538), direct message (`thread`, `send-message`, `thread`, `unsend-message`, `thread` on thread_fbid 1071167815895546). The probe spaces the processes itself, 2.85 s after each command and 30 s plus a mean 5 s jitter between writes, and keeps its own write stop, because each process has a fresh pacer. It refuses to write over a note already up, on a post that is not the owner's, or on a target that is not `IG_FOLLOW_TARGET` or is already followed. Ran 2026-09-23: 26 requests all 200, 352.9 s, every command exit 0, all five cycles `restored` true, log `phase3-cli-acceptance-2026-09-23-160619.json`. Reads `IG_NOTE_TEXT`, `IG_COMMENT_TEXT`, `IG_DM_TEXT` and `IG_FOLLOW_TARGET` from the root `.env` and records none of them. Refuses to run without `--approve phase3`. Run it with `uv run python probes/phase3_cli_acceptance.py --approve phase3`, no `--no-project`. |
| `doctor_bundle_host.py` | 3: the home document, paced, then two bundles 2.85 s apart, cookieless, to `static.cdninstagram.com` | E1 item 7 of the web parity plan, the first engine fetch behind finding `static-js-bundle-fetch`, which rests on two browser captures until this runs. Loads the home document through the account's pinned transport, lists the bundles it names with the library's own `bundle_urls`, and fetches the first two through the canary's cookieless transport pinned to the static host with `build_bundle_request`. Records the document's size and how many bundles it names and how many plainly, and per bundle the status, content type, content encoding, whether a `set-cookie` came back, the decoded size, how many `_instagramRelayOperation` id modules and `.graphql` artifacts `compiled_operations` read, and which registry operations it found and whether their ids match. No document or bundle text, no account id and no token is written. Ran 2026-09-24: the document 200 naming 556 bundles, 9 plainly, both bundles 200 with no `set-cookie`, 4 id modules in the second, and the pass it recorded is the finding's first engine verification. Log `doctor-bundle-host-2026-09-24-001812.json`. Run it with `uv run python probes/doctor_bundle_host.py`, no `--no-project`. |
| `doctor_drift_replay.py` | 5: two home timeline pages, the viewer's profile by id, two profile posts reads | E1 item 7, the two live replays per read that moving a drifted read to a new `doc_id` needs. Reads the drifted ids out of a `dumpsta doctor --live` JSON log given with `--doctor-log`, swaps them into the feed and profile posts query constants for this process only, and replays each twice through its capability's own builder and mapper: the feed's first page and the page after it by its cursor, and the profile posts query at count 1 and 12 for the viewer's own username, read off the profile query first. Stops on a checkpoint or a throttle and retries nothing. Records the `doc_id` sent, status, length, item counts, cursor length and whether the id read back is the viewer's. Ran 2026-09-24, 5 requests, every replay 200 and mapped. Log `doctor-drift-replay-2026-09-24-002208.json`. Run it with `uv run python probes/doctor_drift_replay.py --doctor-log logs/doctor-live-<stamp>.json`. |
| `e2_bundle_artifacts.py` | 0 to Instagram, at most 80 cookieless bundle fetches to `static.cdninstagram.com`, 1.2 s apart | E2 preparation: the compiled artifact of every E2 operation read over plain HTTP, for argument defaults and the fields its normalization selection names, plus any operation or REST path a lazily loaded chunk carries. No page document is fetched: bundle URLs come from the recorded home document capture, first the chunks its bootloader `compMap` lists for eight lazy dialogs, then the bundles that load fetched. The extraction, `doc_id` values included, goes to the reverse-engineer run directory given by `--run`, which is local; the log carries counts and names only. `--start` and `--budget` resume a plan without passing the cap. Ran 2026-09-23 in two parts: 5 fetches with one 30 s read stall, then 73, 78 in all, 751 artifacts, 48 of the 85 census E2 operations and three operations no page load parsed. Logs `e2-bundle-artifacts-2026-09-23-232051.json` and `-232701.json`. Run it with `uv run python probes/e2_bundle_artifacts.py --run RUN`. |
| `_e2_support.py` | 0 | Not a probe. What the `e2_*` replay probes share: the hypothesis contract read at run time from the local knowledge base, so no `doc_id` sits in a probe, a replay that refuses to pass its declared count, spaces sends 2850 ms apart and stops on a checkpoint, throttle, authentication failure or HTML shell, a REST request shaped as the recorded browser ones, and a key union logger that writes no content. |
| `e2_direct_read.py` | 8, up to 3 more conditional | E2 batch 1: the inbox listing's next page, the message requests and a thread's details panel, each replayed twice. Not yet run. Run it with `uv run python probes/e2_direct_read.py`. |
| `e2_profile_tabs.py` | 12, up to 4 more conditional | E2 batch 2 on the owner's own profile: the posts grid and highlights tray past their first pages, and both suggested account lists. Not yet run. Run it with `uv run python probes/e2_profile_tabs.py`. |
| `e2_follow_lists.py` | 6, 1 more conditional | E2 batch 3: one page of the owner's own followers over REST and the `show_many` statuses beside it, twice each, and a second page by an inferred `max_id`. Not yet run. Run it with `uv run python probes/e2_follow_lists.py`. |
| `e2_post_depth.py` | 13, up to 7 more conditional | E2 batch 4 on one timeline post: comment replies and their next page, likers, the post by media pk with its tag, location and coauthor keys, the modal context and the more posts grid. Not yet run. Run it with `uv run python probes/e2_post_depth.py`. |
| `e2_stories.py` | 8, up to 4 more conditional | E2 batch 5: the stories tray, the owner's highlights and own reel, and the gallery, with no seen mutation ever. `--third-party-reel` adds one other account's reel, read only, under W42. Not yet run. Run it with `uv run python probes/e2_stories.py`. |
| `e2_own_account.py` | 7, up to 3 more conditional | E2 batch 6: pending follow requests and the activity feed over REST without `news/inbox_seen`, and the saved collections list. Not yet run. Run it with `uv run python probes/e2_own_account.py`. |
| `e2_discovery_feeds.py` | 9, up to 5 more conditional | E2 batch 7: the explore grid over REST, a location's header and grid, and the new posts check. Not yet run. Run it with `uv run python probes/e2_discovery_feeds.py`. |
| `e2_search.py` | 7, up to 3 more conditional | E2 batch 8: recent searches, the non-personalised typeahead and a hashtag header. Not yet run. Run it with `uv run python probes/e2_search.py`. |
| `e2_page_models.py` | 10, up to 4 more conditional | E2 batch 9: the post page and inbox load companions no finding backs yet. Not yet run. Run it with `uv run python probes/e2_page_models.py`. |
| `posting_discovery.py` | 7 in the photo stage and 8 in the carousel stage, one more each with a bootstrap, 3 and 4 of them writes | E1 item 9's engine-side discovery, the second observation of the upload, the carousel publish and the delete after the browser's, and the first two of the single photo publish, which the browser never sent. Each stage reads the viewer's profile for `media_count`, publishes one generated solid colour 1080 square JPEG (two for the carousel) with an empty caption through `publish_photo` or `publish_carousel` in `_core/writes/posts.py`, reads the post back by its code, deletes it with `delete_post`, reads the code once more to record what a deleted post answers, and reads the profile again. Stops on `CheckpointRequired`, and on any other failure sends only the one delete a post that is up needs. Ran 2026-09-23 with the owner's approval: photo, 7 requests, log `posting-discovery-photo-2026-09-23-225654.json`; carousel, 8, log `posting-discovery-carousel-2026-09-23-230004.json`; photo again, 7, log `posting-discovery-photo-2026-09-23-230134.json`. Every publish read back with the same `pk`, every delete answered `did_delete` true, every read after a delete was refused with `1675030`, and `media_count` was 8 before and after each. Needs `pillow`, a dev dependency. Refuses to run without `--approve posting`. Run it with `uv run python probes/posting_discovery.py --approve posting --stage photo`. |
| `posting_cli_acceptance.py` | 13, one more per re-bootstrap, 16 at most, 7 of them writes | E1 item 9's stop condition from the `dumpsta` command: `profile --by-id`, `publish-photo`, `delete-post`, `publish-carousel` with two slides, `delete-post`, `profile --by-id`, as `uv run dumpsta --json` subprocesses counted by `cli_request_counter/`. Spaces commands itself, 2.85 s after each and 30 s plus a mean 5 s of jitter between writes, because each process has its own pacer. Stops on a checkpoint exit, sends at most one delete for a post that is up after any other stop, and prints a post left up with its code. Ran 2026-09-23 with the owner's approval: 13 requests, 10 to www and 3 to i.instagram.com, both publishes confirmed by their read back, both deletes `gone` on `1675030`, `media_count` 8 before and after. Log `posting-cli-acceptance-2026-09-23-233411.json`. Refuses to run without `--approve posting`. Run it with `uv run python probes/posting_cli_acceptance.py --approve posting`. |
| `_probe_support.py` | 0 | Not a probe. The `.env` loader, the log writer and the page counter the Step 9 probes share. `end_to_end_read.py` keeps its own copies, because rewriting verified evidence costs two live requests to re-establish. |
| `live_repro_bootstrap_page.py` | 2 | Does bootstrap token extraction plus one `useIGDMessageListPaginationQuery` page still work from Python. Written up in [live-reproduction-2026-09-20.md](../../docs/knowledge/live-reproduction-2026-09-20.md) and replayed in [live-reproduction-2026-09-21.md](../../docs/knowledge/live-reproduction-2026-09-21.md). |

Run it with:

```
uv run --with httpx --no-project python probes/live_repro_bootstrap_page.py
```

`--no-project` matters. A probe is not part of the package and does not use its environment, so
without it uv tries to resolve the engine's own project first.

## A probe nothing reruns is a probe whose claims expire

Learned the hard way on 2026-09-21. `live_repro_bootstrap_page.py` resolved `.env` with
`parents[3]`, which was correct under the old `module/` directory name and pointed at
`~/Documents` after the rename to `engine/`. It failed on the first line that touched the file
system, and the break had sat there for a day because nothing had rerun it.

Two consequences worth carrying. The command that runs a probe belongs in
[../docs/engineering/project-profile.md](../docs/engineering/project-profile.md) with its last
verified result, like every other command. And a probe's evidence is only as current as its last
run, so a document citing one should say when it last ran rather than that it exists.
