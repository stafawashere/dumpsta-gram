# Release record, 1.1.0

Filling this record does not authorize publication. Publication needs the user, in the session that
performs it.

- Intended version: `1.1.0`, set in `engine/pyproject.toml` and in `engine/uv.lock`
- Source state: the commit tagged `v1.1.0`. The artifacts below were built from the working tree
  that commit records, before it was made, on top of `f6ae9c5`, with one untracked directory
  present in the checkout, `engine/docs/demo/`, which the sdist's allow list keeps out
- Destination: none configured
- Authorization: the owner's instruction for the night of 2026-09-23, "At each phase stop
  condition, bump the minor version, tag it, and write the release record and its notes. Prepare
  these releases, do not publish them." E1 of [../web-parity-plan.md](../web-parity-plan.md) is
  the phase, and `1.1.0` is its release. The instruction authorizes preparing the version. It
  does not authorize publishing, and nothing was published
- Build environment: uv 0.11.32 (3010295ae 2026-07-23 aarch64-apple-darwin), CPython 3.12.13,
  `uv lock --offline` reported `Updated dumpstagram v1.0.0 -> v1.1.0`, built with
  `uv build --offline --out-dir dist`, which builds the sdist and then the wheel from the sdist
- License: MIT, unchanged. The wheel carries `dumpstagram-1.1.0.dist-info/licenses/LICENSE`
- Change class under ADR-0011: minor. Every public surface change since `v1.0.0` is an addition

## Phase stop condition

E1's stop condition, as [../web-parity-plan.md](../web-parity-plan.md) states it, one part per row.
No live request was sent while preparing this release. The live evidence below is from the runs
that built E1.

| Part | Evidence | Holds |
|---|---|---|
| The census exists and covers every page type | `e683c29`, [../coverage.md](../coverage.md): 265 operations across all fifteen page types, home, explore, reels, profile, post, stories, inbox, thread, notifications, search, saved, settings, hashtag, location and audio, in `run-2026-09-23-190729`, 13 page loads and 0 engine requests. Thread, search and stories were read from another load's bundle (W14). Explore, notifications and audio show zero or one operation because they load their content over REST routes, which the census does not record (INFERENCE), so the count is a lower bound | Yes, as a lower bound |
| A photo and a two item carousel posted, read back and deleted from `dumpsta` in one run | `44faf04`, `probes/posting_cli_acceptance.py`, log `engine/logs/posting-cli-acceptance-2026-09-23-233411.json`: six `dumpsta` steps each exit 0, the photo `media_type` 1 with 1 upload and the carousel `media_type` 8 with 2 uploads and `read_back_carousel_media_count` 2, each `confirmed_by_read` true, each delete exit 0 with the read after it refused with `1675030` and `gone` true, `media_count_before` 8 and `media_count_after` 8, `media_count_restored` true, 13 requests, 3 of them to `i.instagram.com` | Yes |
| `dumpsta doctor` reports zero drift on a fresh session | `f6ae9c5`, log `engine/logs/doctor-live-2026-09-24-003205.json`, the newest doctor run: summary `drift` 0, `replay_failed` 0, `missing` 14, `reads_sent` 10, `bundles_fetched` 150 of 682 named, `bundles_failed` 0. Both reads the first run at `001836` saw drift on, `PolarisFeedRootPaginationCachedQuery_subscribe` and `PolarisProfilePostsQuery`, read `ok` on their new ids. Missing is not drift (W31) | Zero drift holds. It ran on the existing session file, not a freshly adopted session, so "fresh session" is not shown |
| Every harness exits 0 after the split | Rerun for this release, each without a pipe, see Gates | Yes |
| Two `dumpsta` processes on one account share one write budget, gated offline | `tests/test_pacing_ledger.py`, 14 gates, six across real interpreters, among them `test_two_processes_on_one_session_file_share_one_write_budget` and `test_a_write_stop_seen_by_one_process_refuses_writes_in_another`. `uv run pytest -q tests/test_pacing_ledger.py`, 2026-09-24: `14 passed in 4.19s`, and 16 of 16 mutations of `scripts/verify_ledger_gates.py` red then green in the sweep below. The cross-process gates run the engine's own write path in two interpreters, not the `dumpsta` write commands | Yes, for the engine's write path |
| The flat methods and their namespace aliases answer identically, gated offline | `tests/test_facade_parity.py`, among them `test_every_flat_capability_has_exactly_one_namespace_alias` and `test_a_flat_method_and_its_alias_send_the_same_requests_and_answer_alike_async` and `_blocking`. `uv run pytest -q tests/test_facade_parity.py`, 2026-09-24: `238 passed in 3.22s`, and 26 of 26 mutations of `scripts/verify_parity_gates.py` red then green in the sweep below. The plan's 24 counted every `async def` in `aio.py`. The flat capabilities are 17, each with one alias (W21) | Yes, for 17 |

## Freeze condition

ADR-0011 as amended: no line of the snapshot at the Phase 3 baseline removed or changed, added
lines allowed, and after `1.0.0` an added line is a minor release.

- `uv run python scripts/check_surface_additive.py`, 2026-09-24:
  `positive control: a deleted and a changed baseline line were both reported`,
  `baseline 413793a: 254 lines, current: 536 lines`, `added since the baseline: 282`,
  `surface additive: ok, every baseline line is present`, exit 0
- `git diff v1.0.0 HEAD -- engine/tests/public_surface.txt`: 139 lines added and 0 removed,
  397 lines at `v1.0.0` and 536 now. Five commits touched the file, each additions only:
  `d7ae966` (70), `8383b62` (6), `75c3aec` (47), `598fef1` (1) and `44faf04` (15). By kind the
  added lines are 59 `def`, 37 `field`, 15 `class`, 10 `alias`, 10 `property`, 6 `module` and
  2 `data`. No line of `1.0.0` was removed or changed, so no SemVer major bump is owed

## Gates

Run 2026-09-24 on the working tree above, after the version change and the readme change.

| Gate | Command | Result |
|---|---|---|
| Style | the repository hook | No block on any file written in this pass |
| Format and lint | `uv run ruff format --check` then `uv run ruff check` | `262 files already formatted`, exit 0, then `All checks passed!`, exit 0 |
| Type check | `uv run mypy` | `Success: no issues found in 118 source files`, exit 0 |
| Tests | `uv run pytest -q` | `1010 passed in 18.15s`, exit 0, log `engine/logs/pytest-2026-09-24-004053-2.log` |
| Public surface snapshot | `tests/test_public_surface.py`, `tests/test_surface_additive.py` and the additive check above | Inside the pytest run, and the check above exit 0 |
| Provenance, from the repository root | `uv run --no-project python skills/reverse-engineer/scripts/check_provenance.py` | `provenance: ok, every endpoint literal is backed by a verified finding`, 118 files, 30 persisted queries, 50 verified findings at `verify_count` 2 or more, exit 0 |
| The 17.8 gates | `uv run python scripts/verify_release_gates.py` | 21 of 21 mutations red then green, exit 0, log `engine/logs/mutation-release-2026-09-24-005114.json` |
| Every mutation harness | the 33 `scripts/verify_*_gates.py`, each run on its own without a pipe | 33 of 33 exit 0, 622 mutations in all, each reported `red then green`, from `mutation-behavior-2026-09-24-004151.json` to `mutation-write-safety-2026-09-24-005148.json` in `engine/logs/`. No source file was left changed: `git status` 20 s and more after the sweep showed only this pass's edits |
| Harness exits | `uv run python scripts/check_harness_exits.py` | `every control held` in all 33 harnesses (missing anchor, ambiguous anchor, gate that does not fire), exit 0, log `engine/logs/harness-exits-2026-09-24-005217.json` |
| Live probe, dated | none | No live request was sent for this release |

## Artifacts

| Artifact | sha256 | Validation | Status |
|---|---|---|---|
| `dist/dumpstagram-1.1.0-py3-none-any.whl`, 244064 bytes | `7fa1e9f77474c62de817e82e0f8a73d20753c7cf52c8683a8e14838acf9ead70` | Installed with `uv run --isolated --no-project --offline --python 3.12 --with <wheel>` from a temporary directory outside the checkout, with `socket.connect` and `socket.create_connection` replaced by a refusal before import: imported from a uv cache `site-packages`, `importlib.metadata.version` reported `1.1.0`, `py.typed True`, `dumpstagram.session.clear_write_stop` imported, `dumpsta --help` exit 0 and lists `publish-photo`, `delete-post` and `doctor` | Prepared, verified locally |
| `dist/dumpstagram-1.1.0.tar.gz`, 507242 bytes | `eb4d205470890740ab24afb2b4561d6f780e7f9611197b538c31b603fcb45878` | The wheel above was built from it by `uv build`, using only the declared build requirement `hatchling`, offline from the uv cache | Prepared, verified locally |

The artifacts sit in `engine/dist/`, which `engine/.gitignore` ignores, beside the `1.0.0` pair.
They are not committed and not uploaded. Rebuilding from the tagged commit is expected to give the
same contents, and byte identity is not claimed.

- Wheel contents: 124 members, the 118 modules of `dumpstagram/`, `dumpstagram/py.typed`, and
  five `dist-info` files. Gated by
  `tests/test_distribution.py::test_the_wheel_carries_the_package_and_nothing_else`
- Sdist contents: 235 members, `dumpstagram/` (119), `tests/` (65) without
  `tests/fixtures/thread_oracle/`, `scripts/` (37), the eight documents the allow list names,
  `LICENSE`, `uv.lock`, `.python-version`, `.gitignore`, `pyproject.toml` and `PKG-INFO`. The
  owner's untracked `docs/demo/` is not in it. Gated by
  `tests/test_distribution.py::test_the_sdist_ships_nothing_the_repository_keeps_local`
- Credential absence: the gate's own scan, `known_credentials`, `wheel_members`, `sdist_members`
  and `credential_hits` from `tests/test_distribution.py`, was pointed at the two artifacts in
  `dist/`. 11 known values sought, 0 hits in the wheel's 124 members and 0 in the sdist's 235.
  Positive control: each of the 11 values planted compressed beside each archive's members was
  reported, 0 missed in either
- `py.typed` present and visible to a consumer: yes, above, and gated by
  `tests/test_distribution.py::test_the_wheel_carries_py_typed_and_a_consumer_type_checks_against_it`
- Distribution readme: `docs/overview.md`. Its install lines now name `v1.1.0` and
  `dumpstagram-1.1.0-py3-none-any.whl`, and "What has actually been reached" is as of `1.1.0`.
  The wheel's `METADATA` carries it
- Base install and each extra: base install verified as above. The `cli` extra is declared and
  empty, and was not exercised separately
- Platforms exercised: aarch64 macOS, CPython 3.12.13. Intel is unverified until the project's
  final step, per ADR-0010
- Release notes, including upstream endpoint repairs: [1.1.0-notes.md](1.1.0-notes.md)
- Known limitations carried into this release: listed in the notes

## State

- Prepared, not published. No destination is configured, and nothing was uploaded anywhere
- Not committed or tagged by the session that prepared it. The commit and the `v1.1.0` tag are
  made by the orchestrating session
- Published identity, verified values only: none
- Post-publication clean install: not run, nothing was published
- Recovery procedure and owner: none needed until something is published. The owner decides the
  destination and performs any publication

Guidance: [release and compatibility](../engineering/11-release-and-compatibility.md),
[packaging](../engineering/06-packaging-and-dependencies.md).
