# Release record, 1.0.0

Filling this record does not authorize publication. Publication needs the user, in the session that
performs it.

- Intended version: `1.0.0`, set in `engine/pyproject.toml` and in `engine/uv.lock`
- Source state: the commit tagged `v1.0.0`. The artifacts below were built from the working tree
  that commit records, before it was made, with one untracked file present in the checkout,
  `engine/docs/demo.py`, which the sdist's allow list keeps out
- Destination: none configured
- Authorization: the owner's instruction, "The engine is done when the Phase 3 and Phase 4 stop
  conditions in engine/docs/roadmap.md hold and 1.0.0 is cut per ADR-0011". It authorizes cutting
  the version. It does not authorize publishing, and nothing was published
- Build environment: uv 0.11.32 (3010295ae 2026-07-23 aarch64-apple-darwin), CPython 3.12.13,
  `uv lock --offline` reported `Updated dumpstagram v0.4.0 -> v1.0.0`, built with
  `uv build --offline --out-dir dist`, which builds the sdist and then the wheel from the sdist
- License: MIT. `pyproject.toml` declares `license = "MIT"` and `license-files = ["LICENSE"]`,
  and the wheel carries `dumpstagram-1.0.0.dist-info/licenses/LICENSE`

## Freeze condition

ADR-0011 as amended, ruling 1 in 17.13 of `engine/docs/build-plan.md`: no line of the snapshot at
the Phase 3 baseline removed or changed by the close of Phase 4, added lines allowed.

- `uv run python scripts/check_surface_additive.py`, 2026-09-23:
  `positive control: a deleted and a changed baseline line were both reported`,
  `baseline 413793a: 254 lines, current: 397 lines`, `added since the baseline: 143`,
  `surface additive: ok, every baseline line is present`, exit 0
- `git log -p 413793a..HEAD -- engine/tests/public_surface.txt`: nine commits touched the file
  (`ed13dfb`, `8f59d33`, `d7b3242`, `7518750`, `f9d5327`, `0005878`, `1423105`, `a773a1a`,
  `4fe139d`), 144 lines added and 1 removed in all, and `git diff 413793a HEAD` on the file is
  143 additions and 0 removals. The one removed line is
  `field dumpstagram.models.events.EventsDropped.count: int`, changed to `int | None` in
  `0005878`. `EventsDropped` was added in `f9d5327` and is not in the baseline, which has no line
  naming it, so the change touches no baseline line and the freeze holds

## Gates

| Gate | Command | Result |
|---|---|---|
| Style | the repository hook | No block on any file written in this pass |
| Format and lint | `uv run ruff format --check .` then `uv run ruff check .` | `158 files already formatted`, `All checks passed!` |
| Type check | `uv run mypy` | `Success: no issues found in 57 source files` |
| Tests | `uv run pytest -q` | `682 passed in 9.39s`, 2026-09-23, after the version, the readme and the sdist scripts |
| Public surface snapshot | `tests/test_public_surface.py`, `tests/test_surface_additive.py` and the additive check above | Inside the 682, and the check above exit 0 |
| Provenance | `uv run --no-project python skills/reverse-engineer/scripts/check_provenance.py` | `provenance: ok, every endpoint literal is backed by a verified finding` |
| The 17.8 gates | `uv run python scripts/verify_release_gates.py` | 21 of 21 mutations red then green, exit 0, log `engine/logs/mutation-release-2026-09-23-171611.json` |
| Harness exits | `uv run python scripts/check_harness_exits.py` | every control held in all 28 harnesses, exit 0, log `engine/logs/harness-exits-2026-09-23-171652.json` |
| Live probe, dated | `uv run python probes/reload_and_call.py` | 2026-09-23, one request, stored tokens 10338 s old were accepted, no re-bootstrap, 20 edges |

## Artifacts

| Artifact | sha256 | Validation | Status |
|---|---|---|---|
| `dist/dumpstagram-1.0.0-py3-none-any.whl`, 159483 bytes | `d6e22f8de9b633ac8955b29e12cb9abecf061114705650fd4b70647deaa91945` | Installed with `uv run --isolated --no-project --offline --python 3.12 --with <wheel>` from a temporary directory outside the checkout, socket connect refused: imported from a uv cache `site-packages`, `importlib.metadata.version` reported `1.0.0`, `py.typed True`, a `Session` built from dummy values, `dumpsta --help` exit 0 | Prepared, verified locally |
| `dist/dumpstagram-1.0.0.tar.gz`, 384111 bytes | `63ea165812c05b8a2c9b31c04d67f02e85dea9cf17ce5ed47292c3cd3adafdec` | The wheel above was built from it by `uv build`, using only the declared build requirement `hatchling`, offline from the uv cache | Prepared, verified locally |

The artifacts sit in `engine/dist/`, which `engine/.gitignore` ignores. They are not committed and
not uploaded. Rebuilding from the tagged commit is expected to give the same contents, and byte
identity is not claimed.

- Wheel contents inspected, and what was excluded: 63 members, the 57 modules of `dumpstagram/`,
  `dumpstagram/py.typed`, and five `dist-info` files. Gated by
  `tests/test_distribution.py::test_the_wheel_carries_the_package_and_nothing_else`, which also
  requires the wheel's modules to equal the source tree's
- Sdist contents: 147 members, `dumpstagram/`, `tests/` without `tests/fixtures/thread_oracle/`,
  the 32 `scripts/` the gates load or the harnesses run, eight tracked documents from `docs/` named one by one, `LICENSE`, `uv.lock`, `.python-version`,
  `.gitignore`, `pyproject.toml` and `PKG-INFO`. Before this pass the sdist carried the whole of
  `docs/`, which put every gitignored agent document (`build-plan.md`, `roadmap.md`,
  `conventions.md`, `README.md` and `engineering/`), the owner's untracked `docs/demo.py` and the
  oracle fixtures of a private thread into it. Nothing had been published, so nothing leaked.
  Gated by `tests/test_distribution.py::test_the_sdist_ships_nothing_the_repository_keeps_local`.
  `docs/releases/` is not shipped, because this record carries the hash of the sdist it would
  sit in. Unpacked and run with `python -m pytest` outside any git checkout, the sdist's suite
  gave `2 failed, 668 passed, 2 skipped, 5 errors`: the five additive freeze gates and the
  additive command line gate read the baseline with `git show`, and the sdist ignore gate asks
  `git check-ignore`, so seven gates need a git checkout. The two skips are the oracle and the
  credential scan, whose inputs are local. Gated since this pass:
  `tests/test_distribution.py::test_every_script_a_shipped_gate_loads_is_in_the_sdist`
- Distribution readme: `docs/overview.md`, as `pyproject.toml` names it, now opens with a legal and
  safety notice, an install line and a quickstart that adopts a session through `dumpsta adopt
  --cookies-file` and reads a profile through `SyncClient`. The wheel's `METADATA` carries it
- Credential absence: the wheel and the sdist were scanned, gz members decompressed, for eleven
  known values taken from `.env` and `engine/state/session.json`, the session cookie, csrf token,
  mid, password, email, `fb_dtsg`, `lsd` and `fr`. Zero hits in either. Positive control: the
  same scan found every value planted, compressed, beside the members of each archive. Gated by
  `tests/test_distribution.py::test_no_credential_this_machine_holds_is_inside_either_archive`
- `py.typed` present and visible to a consumer: yes. Gated by
  `tests/test_distribution.py::test_the_wheel_carries_py_typed_and_a_consumer_type_checks_against_it`,
  where the development environment's mypy 2.3.1 checks a consumer file against the installed
  wheel with `--strict`. With the marker excluded it reports `module is installed, but missing
  library stubs or py.typed marker`
- Base install and each extra: base install verified as above. The `cli` extra is declared and
  empty, so it installs nothing further, and it was not exercised separately
- Platforms exercised: aarch64 macOS, CPython 3.12.13. Intel is unverified until the project's
  final step, per ADR-0010
- Release notes, including upstream endpoint repairs: [1.0.0-notes.md](1.0.0-notes.md)
- Known limitations carried into this release: listed in the notes

## State

- Prepared, not published. No destination is configured, and nothing was uploaded anywhere
- Published identity, verified values only: none
- Post-publication clean install: not run, nothing was published
- Recovery procedure and owner: none needed until something is published. The owner decides the
  destination and performs any publication

Guidance: [release and compatibility](../engineering/11-release-and-compatibility.md),
[packaging](../engineering/06-packaging-and-dependencies.md).
