# Clients

`SyncClient` is the blocking surface and `AsyncClient` the awaitable one. Both run the same
implementation and answer alike. Construct either from a session file with `from_session_file`.

::: dumpstagram.client.SyncClient

::: dumpstagram.aio.AsyncClient
